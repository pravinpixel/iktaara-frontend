const textColor: never[] = [];

const primaryColor: never[] = [];

const secondaryColor: never[] = [];

const defaultColor: never[] = [];

const errorColor: never[] = [];

export const colors = {
  text: textColor,
  error: errorColor,
  default: defaultColor,
  primary: primaryColor,
  secondary: secondaryColor,
};
